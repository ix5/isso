/* Currently unused
 * TODO: Find a way to point Jest toward this file if it is not in root dir*/
module.exports = {
  server: {
    command: '.venv/bin/isso -c share/isso-dev.conf',
    port: 8080,
  },
}
